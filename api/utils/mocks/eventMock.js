exports.eventMock = [
  {
    id: 1,
    name: 'Node',
    url: 'event5.com/nodejs',
    day: 1597902641,
    template: 'one',
    user_id: 1,
  },
];

// function createEvent(event) {
//   const newEvent = Object.assign({ id: eventsMock.length + 1 }, event);
//   // event.id = eventsMock.length + 1;
//   eventsMock.push(newEvent);
//   const eventCreated = eventsMock.find((val) => val.id === newEvent.id);
//   return eventCreated;
// }

// module.exports = {
//   eventsMock,
// };
